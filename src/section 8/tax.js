/**
 * Calculates total tax from a given amount
 * @param {number} amount given amount on which tax is to be calculated
 * @returns {number} returns tax amount
 */

export function calculateTax(amount) {
  return amount * 0.3
}
